export enum Receive {
    visible = 'ui:visible',
    start = 'ui:start',
}

export enum Send {
    close = 'ui:close',
    uiLoaded = 'ui:loaded',
    finish = `ui:finish`,
}


export enum Key {
    down = 'keydown',
    up = 'keyup',
    pressed = 'keypress',
}

export enum Mouse {
    click = 'click',
    down = 'mousedown',
    up = 'mouseup',
    move = 'mousemove',
}