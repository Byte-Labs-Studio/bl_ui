export enum Receive {
    visible = 'ui:visible',

    start = 'ui:start',

    keydown = 'ui:keydown',
    keyup = 'ui:keyup',
}

export enum Send {
    close = 'ui:close',

    finish = `%s:finish`,
}
